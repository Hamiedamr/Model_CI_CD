apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: ticket-triage-model
  namespace: model-serving
spec:
  predictor:
    containers:
    - name: kserve-container
      image: ghcr.io/hamiedamr/ticket-triage-api:994fc54f
      ports:
      - containerPort: 8000
      resources:
        requests:
          cpu: 250m
          memory: 512Mi
        limits:
          cpu: '1'
          memory: 1Gi
    minReplicas: 1
    maxReplicas: 3
